{
  "name": "learningdevops",
  "version": "1.0.0",
  "description": "Learning devops tools: docker, terraform ,gcp (and built a caching layer with redis)",
  "main": "index.js",
  "scripts": {
    "dev": "PORT=3000 REDIS='localhost' nodemon index.js",
    "prod": "PORT=4000 REDIS='redis' nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.1",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "nodemon": "^2.0.7",
    "redis": "^3.1.2"
  }
}
